import React, { Component } from "react";

class LoginPage extends Component {
  state = {
    loading: true,
    error: null,
  };

  render() {
    return <div>
        <h1>Login page</h1>
    </div>;
  }
}

export default LoginPage;
